- current_user.groups.each do |group|
  = link_to group_messages_path(group),class: "sider" do
    %ul.sidebar__headline
      %li.sidebar__headline__group
        = group.name
      %li.sidebar__headline__message
        = group.show_last_message

